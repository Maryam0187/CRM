'use client';

/**
 * Component placeholder - auto-open functionality removed
 * GlobalWebCallInterface will only open when agent clicks "Join Call" button
 */
export default function InboundCallHandler() {
  // Auto-open functionality removed - interface opens only when agent clicks "Join Call"
  return null;
}

